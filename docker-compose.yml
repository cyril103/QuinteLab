services:
  quintelab:
    image: nginx:alpine
    container_name: quintelab-site-nginx
    restart: unless-stopped
    ports:
      - "8781:80"
    volumes:
      - /docker/hermes-agent-rd02/data/quinte/site:/usr/share/nginx/html:ro
