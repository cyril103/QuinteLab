services:
  site-test:
    image: nginx:alpine
    container_name: site-test-nginx
    restart: unless-stopped
    ports:
      - "8080:80"
    volumes:
      - /docker/hermes-agent-rd02/data/quinte/site:/usr/share/nginx/html:ro
